

const db = require('../../../utils/sequelizeAuto/db');
  let index = await db.User.findAll({
            offset: 0,
            limit:10,
        });
        ctx.body = {code:200, data:index};
        